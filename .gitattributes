# Fins de ligne normalisées en LF dans le dépôt, quelle que soit la machine.
# Sans ça, un aller-retour Windows / macOS fait apparaître tous les fichiers
# comme modifiés alors que rien n'a changé.
* text=auto eol=lf
